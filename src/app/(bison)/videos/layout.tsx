import { ReactNode } from "react";

export const metadata = {
  title: "Videos | Bison Records",
  description: "Music videos and visual works from Bison Records.",
};

export default function VideosLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
